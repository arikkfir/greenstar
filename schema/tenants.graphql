type Tenant {
    id: ID!
    displayName: String!

    accounts(rootsOnly: Boolean): [Account!]!
    account(id: ID!): Account
    transactions: [Transaction!]!
}

input TenantChanges {
    displayName: String!
}
