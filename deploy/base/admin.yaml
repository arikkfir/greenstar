apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app.kubernetes.io/component: admin
  name: greenstar-admin
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: admin
  name: greenstar-admin
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/component: admin
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: admin
    app.kubernetes.io/role: microservice
    greenstar.kfirs.com/oauth-client: "true"
  name: greenstar-admin
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: admin
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/component: admin
    spec:
      containers:
        - image: ghcr.io/arikkfir/greenstar/backend/admin
          name: admin
          env:
            - name: NEO4J_HOST
              value: greenstar-neo4j
          ports:
            - containerPort: 8000
              name: http
          resources:
            limits:
              cpu: 200m
              memory: 32Mi
            requests:
              cpu: 100m
              memory: 16Mi
      serviceAccountName: greenstar-admin
